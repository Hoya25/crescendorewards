project_id = "yhwcaodofmbusjurawhp"

# Allow Stripe (and other third parties) to call this webhook endpoint without JWT auth.
[functions.stripe-webhook]
verify_jwt = false

# Wallet auth needs public access for challenge-response flow
[functions.wallet-auth]
verify_jwt = false

# Session tracking via sendBeacon (no auth header)
[functions.track-session-end]
verify_jwt = false

# Shopify webhook needs public access
[functions.shopify-webhook]
verify_jwt = false

# Monthly claims distribution - admin only, validated in code
[functions.monthly-claims-distribution]
verify_jwt = false

# MCP server - public API for AI agents
[functions.mcp]
verify_jwt = false

# Wingman audit - admin-only, validated via x-admin-key header
[functions.wingman-crescendo-audit]
verify_jwt = false

# Cross-project sync endpoint - authenticated via SYNC_SECRET header
[functions.receive-lock-request]
verify_jwt = false

# User status export - authenticated via SYNC_SECRET header
[functions.user-status-export]
verify_jwt = false

# Universal auth verification - authenticated via SYNC_SECRET header
[functions.verify-universal-auth]
verify_jwt = false

# Public rewards preview API for cross-platform use
[functions.crescendo-rewards-preview]
verify_jwt = false

# Public NCTR price oracle (Dexscreener wrapper)
[functions.get-nctr-price]
verify_jwt = false

# Reward claim settlement engine - requires user JWT (validated in code)
[functions.process-claim]
verify_jwt = false

# Engine registry sync - called by pg_cron via SYNC_SECRET header
[functions.sync-engine-registry]
verify_jwt = false

# Engine membership check - public, proxies BH (LOCKED_FALLBACK on outage)
[functions.check-engine-membership]
verify_jwt = false

# Engine-aware catalog fetcher - public read of active rewards + membership join
[functions.fetch-engine-aware-catalog]
verify_jwt = false
